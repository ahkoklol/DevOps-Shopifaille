name: Store Gateway CD - Build, Push to GHCR & Docker Hub

on:
  push:
    branches:
      - main # Trigger CD only on merges to main
    paths:
      - "back/store-gateway/**"
      # Path to the deployment file, to be used later
      - "kubernetes/store-gateway-deployment.yaml"

jobs:
  build-and-push:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write # Necessary to push Docker image to GHCR

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      # --- 1. CI/Build (Ensures JAR is built for Docker context) ---

      - name: Setup JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: "temurin"
          cache: "maven"

      - name: Build and Package with Maven
        working-directory: back/store-gateway
        # Run Maven to compile code and produce the JAR in target/
        run: mvn -B package -DskipTests

      # --- 2. Docker Logins ---

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      # Log in to GitHub Container Registry (GHCR)
      - name: Log in to GitHub Container Registry (GHCR)
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      # Log in to Docker Hub
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ github.repository_owner }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      # --- 3. Docker Build and Push to Both Registries ---

      - name: Build and Push Docker image
        uses: docker/build-push-action@v6
        with:
          # Context points to the service directory for Docker to find the Dockerfile, pom.xml, and target/
          context: ./back/store-gateway
          file: ./back/store-gateway/Dockerfile
          push: true
          # Tags for both GHCR and Docker Hub
          tags: |
            ghcr.io/${{ github.repository_owner }}/store-gateway-service:latest
            ${{ github.repository_owner }}/store-gateway-service:latest

      - name: Image Pushed Confirmation
        # Updated confirmation message
        run: echo "Store Gateway image successfully pushed to GHCR and Docker Hub (${{ github.repository_owner }}/store-gateway-service:latest)"